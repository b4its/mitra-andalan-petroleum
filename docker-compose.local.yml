services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.local
    container_name: mandalan-backend
    restart: unless-stopped
    ports:
      - "8012:8012"
    environment:
      DATABASE_URL: mysql+aiomysql://mandalan:mandalan@db:3318/mandalan
    volumes:
      - ./backend:/app
      - media_data:/app/media
    depends_on:
      db:
        condition: service_healthy
    profiles:
      - core
      - full

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.local
    container_name: mandalan-frontend
    restart: unless-stopped
    ports:
      - "8092:3012"
      - "24678:24678"
    environment:
      NUXT_API_PROXY_TARGET: http://backend:8012/api/v1
      NUXT_MEDIA_PROXY_TARGET: http://backend:8012/media
      PORT: "3012"
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.nuxt
      - /app/.output
    depends_on:
      - backend
    profiles:
      - full
